name: amar_proshno
description: A simple Flutter Quiz Application - Amar Proshno

publish_to: 'none'

version: 1.0.0+1
get: ^4.6.6

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true